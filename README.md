# 🐦 PyTweet - Twitter Clone

A lightweight Twitter clone built with **FastAPI** (Backend) and **Vanilla JavaScript/TailwindCSS** (Frontend).

## 🚀 Tech Stack
- **Backend:** Python 3.12+, FastAPI, SQLModel (SQLAlchemy)
- **Database:** PostgreSQL (or SQLite for local dev)
- **Frontend:** HTML5, Vanilla JS, TailwindCSS (CDN)
- **Auth:** BCrypt Password Hashing

---

## 🛠️ Setup Instructions

### 1. Clone the Repository
Open your terminal and run:
git clone hhtps://github.com/maharshiicodes/py-tweet.git
cd Py-tweet
For Linux/Mac:

python3 -m venv .venv
source .venv/bin/activate
For Windows:

Bash
python -m venv .venv
.venv\Scripts\activate

2.Install Dependencies
pip install -r requiremnets.txt

Then run the following command
touch .env

then at last run this
fastapi dev app/main.py
